VOD APPLICATION

Step to run the app:

  1- clone the repository
  2- access the repository from the temrinal
  3- type "npm install" (wait until the process is over)
  4- type "npm start" (A window will open in your default browser and display the localhost: 3000 with the application running)
